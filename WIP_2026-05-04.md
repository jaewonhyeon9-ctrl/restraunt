# 진행 중 작업 — 2026-05-04 저녁

> 다음 세션 시작할 때 이 파일부터 읽으면 바로 이어갈 수 있습니다.

---

## 오늘 완료 + 배포된 것 (Vercel 프로덕션 반영됨)

### 커밋 1 — `b2e3b30` (이미 배포됨)
**feat: GPS 검증 토글 + 직원 직급 UI + 다점포 사장 매장 추가**

- `/api/restaurant` PATCH가 `gpsEnforced` 받게 (토글 저장 안 되던 버그 fix)
- `/api/restaurant` 반경 상한 500m → 2000m, 카드 입력과 일치
- `/api/employees` GET/POST에 role 입출력 추가 (점장/대리/사원)
- `/api/employees/[id]` GET/PATCH/DELETE 동일 처리, PATCH로 직급 변경 가능
- `src/app/(owner)/employees/page.tsx` 폼에 점장/대리/사원 3-button selector + 리스트 직급 뱃지
- `/api/restaurants/create` OWNER 체크를 `UserRestaurant` 기반으로 확장 (다점포 사장이 추가 매장 만들 수 있게)
- `prisma/manual-migration-gps-enforcement.sql` 추적 추가

### 커밋 2 — `573f605` (이미 배포됨)
**feat: 매장명 변경 + 매장 정보 카드에서 새 매장 추가**

- `/api/restaurant` PATCH가 `name` 받게 (1~60자 검증)
- `RestaurantLocationCard`에 매장명 인라인 편집 + 하단 "+ 새 매장 추가" 인라인 폼
- 헤더 `RestaurantSwitcher` 시각 강화 (큰 버튼, 매장 개수 뱃지, 강조된 ▾)
- `restaurants.length === 0 → null` 가드 제거
- `scripts/check-migrations-applied.mjs` 추가 (프로덕션 DB 마이그레이션 확인용)

### 프로덕션 DB 상태 (검증 완료)
- `Restaurant.gpsEnforced` 컬럼 존재 ✓
- `Restaurant.gpsRadius` 기본값 200 ✓
- Role enum: OWNER / MANAGER / DEPUTY / STAFF / EMPLOYEE 모두 있음 ✓
- `UserRestaurant` 테이블 + `User.activeRestaurantId` 컬럼 ✓

---

## 🔄 진행 중 — "매장 관리" 탭 만들기 (반쯤 됨, 커밋 안 됨)

**왜**: 매장 추가 모달이 또 화면에서 잘려서, 모달 대신 풀페이지 탭에서 추가/수정하게 변경 중.

### 이미 만든 파일 (워킹 트리에 있음, 아직 커밋 X)

#### 1. `src/app/api/restaurants/[id]/route.ts` (NEW)
- PATCH: 특정 매장 이름/주소 변경 (해당 매장의 OWNER만)
- 사용 zod 스키마로 name(1~60), address(<=120) 검증

#### 2. `src/app/(owner)/restaurants/page.tsx` (NEW)
- 매장 관리 페이지 — 풀스크린, 모달 없음
- 매장 카드 리스트:
  - 활성 매장 강조 (오렌지 테두리)
  - "주" 뱃지 (isPrimary), "사장/점장" 뱃지
  - "🔄 이 매장으로 전환" / "✎ 이름 변경" 버튼
  - 인라인 이름 편집 (input + 저장/취소)
- 하단 "➕ 새 매장 추가하기" 섹션:
  - 이름 (필수) + 주소 (선택)
  - "지금 매장 위치도 같이 등록" 체크박스 → 켜면 LocationPicker 220px 노출
  - "매장 생성하고 전환하기" 버튼

### 안 끝난 작업 (이거 끝내면 커밋·배포 가능)

#### A. 레이아웃에 탭 추가 — `src/app/(owner)/layout.tsx`
TAB_ITEMS 배열에 매장 추가 + 거래처 아이콘 변경 (🏪 충돌 회피):

```tsx
const TAB_ITEMS = [
  { href: '/dashboard', label: '대시보드', icon: '📊' },
  { href: '/finance', label: '재무', icon: '💰' },
  { href: '/employees', label: '직원', icon: '👥' },
  { href: '/inventory', label: '재고', icon: '📦' },
  { href: '/suppliers', label: '거래처', icon: '🤝' },  // 🏪 → 🤝
  { href: '/restaurants', label: '매장', icon: '🏪' },  // NEW
]
```

> ⚠️ 5탭에서 6탭으로. 모바일에서 좁아질 수 있으니 직접 보고 라벨 줄여야 할 수도 있음.
> 대안: 거래처 아이콘 그대로 두고 매장 아이콘 🏬 사용해도 됨.

#### B. 중복 UI 제거
매장 관리 탭이 새 매장 추가의 정식 위치가 되므로, 다른 곳의 인라인 폼은 제거해서 한 곳에서만 관리:

- **`src/components/owner/RestaurantSwitcher.tsx`**:
  - 모달 안의 "+ 새 매장 추가" 버튼 + creating 상태 + LocationPicker 호출 부분 제거
  - 매장 리스트만 보여주는 단순 전환기로 축소
  - "매장 관리는 매장 탭에서" 같은 안내 추가하면 친절

- **`src/components/owner/RestaurantLocationCard.tsx`**:
  - 카드 하단 "➕ 새 매장 추가 (다점포)" 인라인 폼 섹션 제거
  - `showCreate` / `createName` / `handleCreateRestaurant` 관련 state·함수 제거
  - 매장 이름 인라인 편집은 **유지** (대시보드에서 빠른 변경용)

#### C. 타입 체크 + 커밋 + 푸시

```bash
cd "/c/Users/Hyun Jae Won/Desktop/restaurant-app"
npx tsc --noEmit
git add src/app/api/restaurants/\[id\]/route.ts \
        src/app/\(owner\)/restaurants/page.tsx \
        src/app/\(owner\)/layout.tsx \
        src/components/owner/RestaurantSwitcher.tsx \
        src/components/owner/RestaurantLocationCard.tsx
git commit -m "feat: 매장 관리 탭 분리 + 풀스크린 추가/편집"
git push
```

---

## ⚠️ 중단 시점 — 사용자가 거부한 마지막 작업
`src/app/(owner)/layout.tsx`에 매장 탭 추가하는 Edit이 reject 됐음.
탭 6개로 늘어나는 게 모바일에서 어색할 수 있어서 사용자가 직접 보고 결정하고 싶을 가능성 큼.
다음 세션에서 사용자에게 6탭 OK인지 물어보고 진행할 것.

---

## 다른 우선순위 (메모리 NEXT_STEPS.md 기준, 변동 없음)

식당앱 마저 정리하고 나면:
1. **퇴근 사진 체크리스트 UI** — 스키마 적용됨, UI 미작업
2. **메뉴/레시피/원가율 UI** — 스키마 적용됨, UI 미작업
3. **체크메이트(별개 프로젝트)** — 카카오 알림톡 cron만 남음
   - 폴더: `c:\Users\Hyun Jae Won\.antigravity\aftermind-checkmate`
   - 라이브: https://aftermind-checkmate.vercel.app

---

## 모두의 창업 지원서 진행 상황

오늘 작성 완료 (사용자가 폼에 입력 중이었음):
- Q1 (한 줄 아이디어), Q2 (배경), Q3 (문제 해결), Q4 (실현 계획) — 모두 1000자 한도 안에서 작성
- Q5 (사업 분야), Q6 (창업 여부)는 사용자 결정 대기
- Q8 (영상)은 추후

질문지 답변 원본은 채팅 기록에 있음. 필요하면 다시 정리해서 출력 가능.

---

## 빠르게 다시 시작하려면

1. 이 파일 읽기 (현재 읽고 있음)
2. 위 "안 끝난 작업" 섹션 A → B → C 순으로 진행
3. 6탭 OK 여부만 사용자에게 확인하면 바로 마무리 가능

핵심 파일 다시 열려면:
- [src/app/(owner)/restaurants/page.tsx](src/app/%28owner%29/restaurants/page.tsx)
- [src/app/(owner)/layout.tsx](src/app/%28owner%29/layout.tsx)
- [src/components/owner/RestaurantSwitcher.tsx](src/components/owner/RestaurantSwitcher.tsx)
- [src/components/owner/RestaurantLocationCard.tsx](src/components/owner/RestaurantLocationCard.tsx)
